name: Docker Build Stable

# This workflow is now handled by build_push.yml
# Keeping this file to avoid breaking references, but it does nothing

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docker builds are now handled by build_push.yml"
