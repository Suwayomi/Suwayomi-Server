package ireader.core.http

object WebViewUtil {
    const val SPOOF_PACKAGE_NAME = "org.chromium.chrome"
    const val MINIMUM_WEBVIEW_VERSION = 99
    // Use the shared DEFAULT_USER_AGENT constant from DefaultUserAgent.kt
}